#include "vector.h";

//////////////////////////////////////////////////////////////////
// Declare a new type to hold camera information
/////////////////////////////////////////////////////////////////
typedef struct
{
    vec3_t position;
    vec3_t rotation;
    vec3_t fov_angle;
} camera_t;

//////////////////////////////////////////////////////////////////
// Create a new camera variable and intialize its values
/////////////////////////////////////////////////////////////////
camera_t mycam = {
    {0, 0, 0},
    {0.3, -2.0, 0.0},
    0.78
};